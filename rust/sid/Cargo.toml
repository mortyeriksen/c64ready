[package]
name = "sid"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "reSID SID chip translation (WASM engine for the c64 emulator)"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
